--- src/intel_device.c.orig	2014-06-24 13:30:56.000000000 +0200
+++ src/intel_device.c	2014-07-24 21:14:41.000000000 +0200
@@ -314,8 +314,8 @@
 		 pci->domain, pci->bus, pci->dev, pci->func);
 
 	ret = drmCheckModesettingSupported(id);
-	if (ret) {
-		if (xf86LoadKernelModule("i915"))
+	if (ret || 1) {
+		if (xf86LoadKernelModule("i915kms"))
 			ret = drmCheckModesettingSupported(id);
 		if (ret)
 			return -1;
