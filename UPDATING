- We restart the xorg report.

20110409
    mesa stuff is works now, but need a lot cleanup
    USE_PYTHON and py-libxml2 is now must have dependens
    it should go to bsd.mesalib
    dri patch-configre should be global